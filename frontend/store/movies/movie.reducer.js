/**
 * @copyright Popcorn, 2018
 * @author Calvin Feng
 */

const defaultState = {
  mostViewed: {}
};

export default function movieReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
