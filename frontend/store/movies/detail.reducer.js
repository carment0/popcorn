/**
 * @copyright Popcorn, 2018
 * @author Calvin Feng
 */

const defaultState = {};

export default function movieDetailReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
