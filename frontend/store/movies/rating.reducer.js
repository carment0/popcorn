/**
 * @copyright Popcorn, 2018
 * @author Calvin Feng
 */

const defaultState = {};

export default function movieRatingReducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
